# הוספת גיליון שבועי — Ai Bina

האתר הוא HTML סטטי פשוט — בלי בנייה, בלי npm. כל גיליון הוא קובץ `.html` נפרד.

## התהליך (כ־10 דקות)

1. העתיקו את קובץ הגיליון האחרון לשם חדש לפי תאריך הפרסום:

   ```bash
   cp posts/2026-08-14.html posts/YYYY-MM-DD.html
   ```

2. בקובץ החדש `posts/YYYY-MM-DD.html`:
   - עדכנו את `<title>` ואת ה־`<meta name="description">` בראש הקובץ.
   - עדכנו את הכותרת `<h1 class="neon-text">` בגוף העמוד.
   - מחקו את תוכן ה־`<article class="post-body">` הישן והזינו במקומו את הסיכום החדש:
     - קטגוריה → `<h2>שם הקטגוריה</h2>`
     - ידיעה ממוספרת → `<h3>N. כותרת הידיעה</h3>`
     - כל ידיעה → `<p>` אחד עם המשפטים מופרדים ב־`<br>`
     - מונחים באנגלית נשארים באנגלית; שמות קבצים/פקודות עטופים ב־`<code>...</code>`

3. הוסיפו כרטיס חדש בראש הרשימה ב־`index.html` (בתוך `<ul class="editions">`):

   ```html
   <li>
     <a class="edition-card" href="/posts/YYYY-MM-DD">
       <div class="edition-date">DD בחודש YYYY</div>
       <h2 class="edition-title">סיכום חדשות ה־AI — שבוע DD בחודש YYYY</h2>
       <p class="edition-desc">משפט אחד שמסכם את השבוע.</p>
     </a>
   </li>
   ```

4. בדיקה מקומית — פתחו את `index.html` ישירות בדפדפן, או הריצו שרת מקומי:

   ```bash
   npx serve .
   ```

5. פרסום:

   ```bash
   git add -A
   git commit -m "post: גיליון YYYY-MM-DD"
   git push
   ```

   Vercel יפרוס אוטומטית תוך כדקה בזכות החיבור ל־GitHub.

## מבנה קובץ הגיליון

- כל קובץ גיליון עומד בפני עצמו — כולל header, footer וקישור לגיליון הקודם (`/`).
- אין תבנית משותפת מעבר לגיליון האחרון שמעתיקים ממנו — זו הפשרה שנבחרה כדי להימנע מכלי בנייה.
- העיצוב (צבעים, גופנים, אנימציית הקו הניאון) מגיע כולו מ־`assets/style.css` המשותף — לא צריך לגעת בו מדי שבוע.
